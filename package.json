{
  "name": "smart-search",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "npx tsc --build && cp ./src/generated ./dist/ -r",
    "start": "node dist/index.js",
    "import": "node dist/import.js",
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "csv-parse": "^5.5.6",
    "fs": "^0.0.1-security",
    "prisma": "^5.2.0",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  },
  "keywords": [],
  "author": "Laczko-Albert Tamas",
  "engines": {
    "node": ">=21.0.0"
  },
  "engineStrict": true
}
